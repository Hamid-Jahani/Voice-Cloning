# Runtime dependencies.
#
# Bark and the HuBERT quantizer are installed from git rather than PyPI, so
# they cannot live in pyproject.toml's [project.dependencies] if the package is
# ever published. Install this file directly for a working environment:
#
#   pip install -r requirements.txt
#
# A CUDA GPU is strongly recommended. On CPU, generation is roughly an order of
# magnitude slower and long-form narration becomes impractical.

# --- core numerical / audio stack -------------------------------------------
numpy>=1.21
scipy>=1.7
torch>=1.13
torchaudio>=0.13
nltk>=3.8

# --- Bark and the voice-cloning quantizer -----------------------------------
git+https://github.com/suno-ai/bark.git
git+https://github.com/gitmylo/bark-voice-cloning-HuBERT-quantizer

# --- optional -----------------------------------------------------------------
# 24-bit FLAC output (voice_cloning.audio.write_flac):
#   pip install soundfile>=0.12
#
# NISQA audio-quality scoring is a git checkout driven as a subprocess, not a
# package. See voice_cloning/quality.py:
#   git clone https://github.com/gabrielmittag/NISQA.git

# --- development ---------------------------------------------------------------
# pip install -e ".[dev]"
